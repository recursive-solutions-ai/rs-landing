/**
 * Shared TypeScript types for the SaaS Starter.
 *
 * These are application-level types that extend or wrap Prisma-generated types.
 * They provide the contract between frontend and backend, ensuring type safety
 * across API boundaries.
 *
 * NEVER use `any` — use `unknown` + type narrowing if needed.
 */

export * from "./api"
export * from "./auth"
export * from "./user"
export * from "./blog"
export * from "./billing"
export * from "./storage"
